
# 暴露监控端点
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    bus-env:
      enabled: true
    bus-refresh:
      enabled: true
    health:
      show-details: always
